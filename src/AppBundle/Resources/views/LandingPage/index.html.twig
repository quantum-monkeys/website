{% extends 'AppBundle::base.html.twig' %}

{% block body %}
    <div class="container landing-page">
        <div class="row">
            <div class="hidden-xs col-sm-4">
                <picture>
                    {% media landingPageTranslation.picture, 'landing_page_main' with {
                        'class': 'img-responsive'
                    } %}
                </picture>
            </div>
            <div class="col-xs-12 col-sm-8">
                <h2>{{ landingPageTranslation.title }}</h2>

                {{ landingPageTranslation.intro|markdown }}

                <h3>{{ landingPageTranslation.formTitle }}</h3>

                {{ form_start(form, {
                    'attr' : {
                        'class': 'form-horizontal'
                    }
                }) }}
                    <div class="row">
                        <div class="col-xs-6">
                            {{ form_widget(form.firstName, {
                                'attr' : {
                                    'class': 'form-control'
                                }
                            }) }}
                        </div>
                        <div class="col-xs-6">
                            {{ form_widget(form.lastName, {
                                'attr' : {
                                    'class': 'form-control'
                                }
                            }) }}
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-xs-12">
                            {{ form_widget(form.email, {
                                'attr' : {
                                    'class': 'form-control'
                                }
                            }) }}
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-xs-12">
                            {{ form_widget(form.company, {
                                'attr' : {
                                    'class': 'form-control'
                                }
                            }) }}
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-xs-12">
                            {{ form_widget(form.submit, {
                                'label': landingPageTranslation.submitButtonLabel,
                                'attr' : {
                                    'class': 'btn btn-default'
                                }
                            }) }}
                        </div>
                    </div>
                {{ form_end(form) }}
            </div>
            <div class="visible-xs col-xs-12">
                <picture>
                    {% media landingPageTranslation.picture, 'landing_page_main' with {
                        'class': 'img-responsive'
                    } %}
                </picture>
            </div>
        </div>
    </div>
{% endblock %}
